# Backend (`apps/backend`)

Reserved area for backend services and APIs.

Current status: scaffold only.

Guidelines:
- Keep service code under `src/`.
- Keep contracts aligned with `apps/web/lib/api/client.ts`.
- Do not place frontend or on-chain logic here.
