# On-chain Program (`programs/airbag`)

Reserved area for Solana program code and tests.

Current status: scaffold only.

Suggested layout:
- `program/` for source code
- `tests/` for program/integration tests
